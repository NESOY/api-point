spring:
  profiles:
    active: local


---
spring:
  profiles: local
  datasource:
    url: jdbc:mysql://localhost:3306/point?serverTimezone=UTC
    username: root
    password:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop # create-drop, create
